<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado presionado -->
    <item android:drawable="@color/menuItemPressed" android:state_pressed="true"/>
    <!-- Estado seleccionado -->
    <item android:drawable="@color/menuItemPressed" android:state_selected="true"/>
    <!-- Estado por defecto -->
    <item android:drawable="@android:color/transparent"/>
</selector>
